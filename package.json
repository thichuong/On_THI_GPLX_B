{
  "name": "on-thi-gplx-600",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "author": "Thi Thanh Chuong <thichuong22022000@gmail.com>",
  "license": "MIT OR Apache-2.0",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build && node scripts/update-sw-manifest.js",
    "preview": "vite preview",
    "test": "node --test tests/**/*.test.js",
    "deploy": "npm run build && npx wrangler deploy",
    "deploy:pages": "npm run build && npx wrangler pages deploy dist --project-name=on-thi-gplx-600"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
